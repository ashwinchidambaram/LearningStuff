#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>

int main(){
    double width = 4;
    double height = 10;
    double perimeter = 0.0;
    double area = 

    perimeter = 2 * (width + height);
    area = width * height;

    printf("The perimeter is: %f \n", perimeter);
    printf("The area is: %f", area);

    return 0;
}